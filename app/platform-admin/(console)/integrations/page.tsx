export const dynamic = "force-dynamic";
import { StubPage } from "../_stubs/stub-page";
export default function Page() {
  return StubPage({ title: "Integrations", description: "Platform-level integration provider registry and connector catalog." });
}
