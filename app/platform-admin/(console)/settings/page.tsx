export const dynamic = "force-dynamic";
import { StubPage } from "../_stubs/stub-page";
export default function Page() {
  return StubPage({ title: "Platform Settings", description: "Global platform configuration, limits, and operational parameters." });
}
