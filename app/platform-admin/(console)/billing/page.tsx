export const dynamic = "force-dynamic";
import { StubPage } from "../_stubs/stub-page";
export default function Page() {
  return StubPage({ title: "Billing & Invoices", description: "Platform-wide billing overview and invoice management." });
}
