export const dynamic = "force-dynamic";
import { StubPage } from "../_stubs/stub-page";
export default function Page() {
  return StubPage({ title: "Security Center", description: "Platform-level security: MFA enforcement, IP allowlists, and threat detection." });
}
